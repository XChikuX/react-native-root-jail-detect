pre-commit:
  parallel: true
  commands:

    lint:
      glob: "*.{js,ts,jsx,tsx}"
      run: bun x eslint {staged_files}

    types:
      glob: "*.{js,ts,jsx,tsx}"
      run: bun x tsc
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: bun x commitlint --edit
